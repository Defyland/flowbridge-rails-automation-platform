# Benchmark Results

Store k6 JSON summaries and text reports here. `bin/benchmark` writes one summary export and one text report per scenario, while the raw Rails server log stays in the system temp directory to keep repository artifacts small enough for review. Summarize the important numbers in `benchmarks/baseline.md`.
